{
  "name": "barber-team-machine",
  "version": "0.1.0",
  "private": true,
  "description": "Sistema web inteligente para la gestion de Barber Team Machine",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace backend\" \"npm run dev --workspace frontend\"",
    "install:all": "npm install",
    "test": "npm run test --workspaces --if-present"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
